export default function Preview0aLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="h-dvh min-h-0 w-full overflow-hidden bg-black">
      {children}
    </div>
  );
}
