# 来源（ClawHub）

- 页面：[springer63/implement-design](https://clawhub.ai/springer63/implement-design)（v1.0.0，MIT-0）
- ZIP：`https://wry-manatee-359.convex.site/api/v1/download?slug=implement-design`

## 使用注意

- **依赖 Figma MCP**：`SKILL.md` 假定已连接 Figma MCP，并能使用如 `get_design_context`、`get_screenshot`、`get_metadata` 等工具；若不可用需在本机/客户端侧启用对应插件或 MCP，并自行确认是否需要额外凭证。
- **资源 URL**：说明中可能通过 **localhost** 提供设计资源；仅在信任该端点与网络配置时使用，避免误连本地敏感服务。
- 包内仅含指令型 `SKILL.md`；文中提到「插件内置 MCP」与压缩包内无安装文件时，以你实际环境文档为准。
